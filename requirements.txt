# Web
fastapi
uvicorn[standard]
jinja2
python-multipart          # form posty (login, registrace, editor)
itsdangerous              # podepsané session cookie (Starlette SessionMiddleware)

# DB
sqlalchemy>=2.0
psycopg[binary]           # PostgreSQL v produkci (lokálně stačí vestavěné sqlite3)

# Auth
bcrypt                    # hashování hesel napřímo (bez passlibu — Py3.14/bcrypt5)

# Obsah / render
pyyaml                    # front matter při importu katalogu
markdown                  # render answer_md -> HTML

# Config
python-dotenv
