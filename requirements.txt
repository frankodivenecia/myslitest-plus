# Web
fastapi
uvicorn[standard]
jinja2
python-multipart          # form posts (login, editor)
itsdangerous              # signed session cookies (Starlette SessionMiddleware)

# DB
sqlalchemy>=2.0
psycopg[binary]           # PostgreSQL v produkci (lokálně stačí vestavěné sqlite3)

# Auth
passlib[bcrypt]

# Obsah / render
pyyaml                    # front matter při importu katalogu
markdown                  # render answer_md -> HTML

# Config
python-dotenv
